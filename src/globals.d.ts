// Build-time constants injected by Vite's `define` (see vite.config.ts). Shown on
// the About screen and used as the contribution app version.
declare const __APP_VERSION__: string;
declare const __APP_GIT_SHA__: string;
declare const __APP_BUILT_AT__: string;
